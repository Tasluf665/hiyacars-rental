const Colors = {
  // Primary Dark Color: Used for main text, headings, and primary CTA buttons ("Login", "Get Started", "Pay Now").
  Primary: "#181725",

  // Accent Color: A clean, modern blue for links, active states (e.g., selected dates), and visual highlights.
  Accent: "#007AFF",

  // Backgrounds
  White: "#FFFFFF",
  BackgroundLight: "#F9F9F9", // Used for subtle background separation, like the difference between cards and the main screen.

  // Text Colors
  TextPrimary: "#181725", // Same as Primary for consistent major text color.
  TextSecondary: "#7C7C7C", // Used for descriptions, secondary details, and placeholder text (matches the gray in your UI).

  // Input and Border Colors
  InputBackground: "#F6F6F6", // A clean light gray for input fields (as seen on the booking screen).
  Border: "#E2E2E2", // For divider lines and input borders.
};

export default Colors;
